[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head && python start.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
